<h1>投稿一覧</h1>

<% @books.each do |book| %>

  <p>タイトル</p>
  <span>
    <%= link_to "Show", book_path(book.id) %> | <%= link_to "Edit", book_path(book.id) %> | <%= link_to "Destroy", book_path(book.id) %>
  </span>
  
<% end %>